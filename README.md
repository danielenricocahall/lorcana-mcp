# Lorcana card data

This branch is published via GitHub Pages and serves the Ravensburger
Lorcana card catalog as `allCards.json`. It is updated daily by the
`Fetch Lorcana Cards` workflow on `main`.
